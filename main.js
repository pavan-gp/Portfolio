var typed = new Typed(".text", {
  strings: ["Developer and UI/UX Designer"],
  typeSpeed: 90,      // use typeSpeed instead of speed
  backSpeed: 40,
  backDelay: 1200,
  loop: false,
  showCursor: false,   // hide cursor (no blink)
  startDelay: 2500    // 1 second delay before typing starts
});
